# .gitignore

# VS Code specific
.vscode/
.vscode-server/
*.code-workspace

# Dev Container specific build/cache files
.devcontainer/cache/

# OS specific files
.DS_Store
Thumbs.db

# Node.js (if used)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# Python (if used)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
*.manifest
*.spec
pip-log.txt
pip-delete-this-directory.txt
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.log
.hypothesis/
.pytest_cache/
cover/
*.mo
*.pot
*.po

# Environment variables
.env
.env.*
!.env.example

# Terraform (if used)
.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Ignore Arista cEOS lab image files (tar, tar.gz, tar.xz)
# These are large binary files imported into Docker/Podman and should not be committed.
[cC]EOS*.tar
[cC]EOS*.tar.gz
[cC]EOS*.tar.xz

# You might want to add other common ignores below:
# Example: Python virtual environment directory defined in post-create.sh
# (Though it's outside the workspace mount by default in this setup)
# ansible-venv/

# Example: Containerlab topology files directory (if created)
clab-*/
